# This file contains provider configuration for the AAP provider
# The actual provider configuration should be done in the root module

# Example provider configuration (commented out as it should be configured by the consumer of this module)
# provider "aap" {
#   host     = var.aap_host
#   username = var.aap_username
#   password = var.aap_password
#   insecure = var.aap_insecure
# }